#include "../core_minimal.h"
#include "../shader.h"
#include "../objectGL.h"

#include "render_manager.h"

void RenderManager::render(Shader* shader, ObjectGL* object)
{
  if(!shader) return;
  if(!object) return;

  
}